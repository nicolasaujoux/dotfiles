import sys

import os

os.path.get("test")

class ExitNotifyThread(Thread):
  """
  This class
  """

  def run(self):
    global exitthreads, profiledir
    self.threadid = thread.get_ident()
    try:
      if not pro

